<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorForApp</item>
        <item name="colorPrimaryDark">@color/colorForTitle</item>
        <item name="colorAccent">@color/colorForApp</item>
    </style>

</resources>
